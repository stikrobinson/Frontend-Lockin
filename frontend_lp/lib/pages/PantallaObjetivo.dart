import 'package:flutter/material.dart';

class Pantallaobjetivo extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Center(
      child: Text("Pantalla Objetivo"),
    );
  }
}